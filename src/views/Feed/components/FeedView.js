import FormAddFeed from "../../../components/Forms/FormAddFeed";
import ListFeeds from "./ListFeeds";

const FeedView = () => {
  return (
    <section>
      <FormAddFeed />

      <ListFeeds />
    </section>
  );
};

export default FeedView;
